/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  // images: {
  //   domains: ["i.ibb.co", "cdn.vectorstock.com"], // <== Domain name
  // },
};

export default nextConfig;
